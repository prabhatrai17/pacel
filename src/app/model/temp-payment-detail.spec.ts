import { TempPaymentDetail } from './temp-payment-detail';

describe('TempPaymentDetail', () => {
  it('should create an instance', () => {
    expect(new TempPaymentDetail()).toBeTruthy();
  });
});
